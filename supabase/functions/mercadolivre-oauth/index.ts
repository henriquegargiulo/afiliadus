// Stub mantido para compatibilidade com o projeto remoto.
// A implementação real virá quando as credenciais OAuth do Mercado Livre forem configuradas.
Deno.serve(() => new Response('Not implemented', { status: 501 }))
